<h2>Listings for <%= @seed.common_name %></h2>
<div id="listings">
	<% @listings.each do |l| %>
		<div class="listing">
			<%= link_to "Listing #{l.id}", listing_path(l) %> submitted by <%= link_to "#{l.lister[:email]}", l.lister  %>
		</div>
	<% end %>
</div>
